# opencode-tps

## Background

OpenCode exposes token counts and total response duration, but it does not show model generation throughput in its TUI. Dividing generated tokens by total response duration is misleading because agent turns can spend most of their time waiting for tools.

## Goals

- Show a weighted tokens-per-second metric for the active OpenCode session.
- Keep metrics for different providers, models, and variants separate.
- Measure model generation time without including tool execution or tool wait time.
- Remain usable when reopening an existing session.

## Functional Requirements

### MVP

- [x] Render the session TPS summary in the OpenCode sidebar.
- [x] Group TPS by provider, model, and variant.
- [x] Count output and reasoning tokens as generated tokens.
- [x] Track generation windows from streaming output events.
- [x] Exclude time outside generation windows, including tool execution.
- [x] Reconstruct approximate historical metrics from stored text and reasoning part timestamps.
- [x] Exclude reverted messages from metrics while an undo is staged and restore them on redo.

### Future

- [ ] Persist precise per-step generation windows across OpenCode restarts.
- [ ] Add optional display and token-accounting configuration.
- [ ] Add per-response TPS if OpenCode exposes a supported assistant footer slot.

## Non-functional Requirements

- Support OpenCode 1.18 or newer within major version 1.
- Avoid a separate SolidJS runtime so the plugin works in packaged OpenCode builds.
- Keep TPS aggregation deterministic and covered by unit tests.
- Do not perform network requests or collect telemetry.

## Architecture

`src/tui.ts` adapts OpenCode TUI events and renders the sidebar. `src/session-tps-state.ts` owns framework-independent generation tracking and weighted aggregation. Event-derived samples replace historical estimates for the same assistant message. A staged revert filters both historical and event-derived samples at OpenCode's message boundary without deleting them, so redo restores the original metrics.

The session metric is:

```text
sum(output tokens + reasoning tokens) / sum(generation window durations)
```

Each model step has its own generation window. Time between steps is never added, so tool execution and tool wait do not affect TPS.

## Milestones

| Milestone | Description                                  | Status   |
| --------- | -------------------------------------------- | -------- |
| M1        | Publishable sidebar plugin with tests and CI | Complete |
| M2        | Public npm and GitHub release                | Planned  |

## Glossary

- **TPS**: Tokens per second generated by the model.
- **Generation window**: Time from the first output delta to the final output-end event for one model step.
- **Historical estimate**: TPS reconstructed from persisted text and reasoning part timestamps when precise stream events are unavailable.

## Change Log

| Date       | Change                        | Author   |
| ---------- | ----------------------------- | -------- |
| 2026-08-20 | Initial requirements          | nextzhou |
| 2026-08-20 | Define undo and redo behavior | nextzhou |
